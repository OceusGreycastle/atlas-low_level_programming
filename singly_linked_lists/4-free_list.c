#include <stdlib.h>
#include <stdio.h>
#include "lists.h"

/**
 *
 *
 *
*/

void free_list(list_t *head)
{
	

	int count = 0;

	if (head != NULL)
	{
		free(str);
		free(head);
		count++;
		return NULL;
	}
}
